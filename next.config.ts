import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "www.themealdb.com",
      "img.spoonacular.com",
    ],
  },
};

export default nextConfig;
